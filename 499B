#include<bits/stdc++.h>
using namespace std;
void ok()
{
    int n,m;
    cin>>n>>m;
    string s2,s1;
    map <string,string> mp;
    while (m--)
    {
        cin >>s1>>s2;
        mp[s1]=s2;
    }
    while (n--)
    {
        cin >>s1;
        if (mp[s1].size()<s1.size())
            cout <<mp[s1]<<" ";
        else
            cout <<s1<<" ";
    }
}
int main()
{
   ok();
   return 0;
}
